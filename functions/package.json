{
  "name": "cleanflow-functions",
  "description": "Cloud Functions Zebramoon — devis PDF + email, CRM auto-feed, API REST, paiement (placeholder).",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0",
    "pdfkit": "^0.15.0"
  },
  "private": true
}
